# dotfiles
My pesonal configuration files

### PowerShell
We place it inside "PowerShell" folder, so that we can immediately move it to `$HOME\Documents` on a fresh
Windows install.

### Wezterm
We place it inside a "wezterm" folder to mimic how it is supposed to be structured inside a `.config` file.
